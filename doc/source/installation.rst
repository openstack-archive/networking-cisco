============
Installation
============

At the command line::

    $ pip install networking-cisco

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv networking-cisco
    $ pip install networking-cisco
